# Release-it Configuration
# Copy this file to .env and fill in your actual tokens

# GitHub Personal Access Token
# Required for creating GitHub releases
# Create at: https://github.com/settings/tokens
# Needs: repo scope for private repos, public_repo scope for public repos
GITHUB_TOKEN=your_github_personal_access_token_here

# NPM Access Token
# Required for publishing to npm registry
# Create at: https://www.npmjs.com/settings/{your-username}/tokens
# Type: Automation token (recommended) or Publish token
NPM_TOKEN=your_npm_access_token_here

# Optional: NPM Registry URL (if using a custom registry)
# NPM_REGISTRY=https://registry.npmjs.org/